# Chaos / failure injection for CI/CD monitoring and root cause analysis testing.
# Leave unset or false for normal operation.

# When true, the application exits on startup (after logging a structured error).
CHAOS_MODE=true

# When true, GET /api/health returns HTTP 500; otherwise returns 200.
# FAIL_HEALTH=true
